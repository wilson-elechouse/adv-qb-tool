#!/usr/bin/env node
console.error('legacy_direct_disabled: submit_billpayment.mjs moved to scripts/deprecated/submit_billpayment.mjs; use submit_from_match_result.mjs via workflow.');
process.exit(1);
